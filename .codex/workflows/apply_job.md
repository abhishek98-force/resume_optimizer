Load and follow `.agents/skills/apply-job/SKILL.md` as the canonical workflow.

Use the command argument as the job-description path. A path written as `/jobs/<file>.txt` may be resolved as project-relative `jobs/<file>.txt` when needed.
